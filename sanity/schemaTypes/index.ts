import { product } from './product'
import { truckModel } from './truckModel'
import { category, hero, testimonial, faq, siteSettings } from './other'

export const schemaTypes = [
  product,
  truckModel,
  category,
  hero,
  testimonial,
  faq,
  siteSettings,
]
